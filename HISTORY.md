# HISTORY.md

Release-to-release changelog for `SLT.pine` — what changed, why, which experiments were tried and reverted, and the relevant commit hashes.

This file is **not** loaded into the model context by default (that's the point of splitting it out of `CLAUDE.md`). `CLAUDE.md` describes only the *current* behaviour of each subsystem; this file is the running history behind it. Append new entries at the end, newest last.

---

## Lineage

The script file is `SLT.pine`. It was forked from **SuperLazyTrade V3** with the SuperTrend signal anchor removed entirely (input option, SuperTrend Engine settings group, adaptive ATR/factor block, `ta.supertrend` call, `st_*` anchor, its plots/fills/circles, dashboard branch). Anchor selection is binary — SMA (default) or EMA Cross. The on-chart `indicator()` title is `SLT V1` (from `VERSION = "V1"`).

---

## Changelog

**Light-build simplification (current):** the input surface is Signal Anchor, Score Stars, P&L Exit, Trading Hours, and two Dashboard & Visuals toggles (`showDash` / `extDash`). Former inputs are auto-tuned from the asset profile + anchor (`ema_slow_auto` / `sma_buf_auto` / `min_score_auto` → `emaSlowPeriod` / `smaBufferATR` / `minScore*`, plus `maxBarsFromFlip` from the anchor) or pinned as constants (`sigTime="Score"`, `gateOn=true`, `rvolMode="Time-of-Day"`, SMA MANUAL mode dropped, success-rate + `showSignalPnL` + `showLabels` + `showCircles`; the `showBg` trend-background toggle was removed outright since the light build has no `bgcolor()`). The **VWAP Fade** module is deleted; the **ORB** module is retained but inert (`orbEnabled=false` constant). (A Component 1 anchor-decoupling — commit `c0d85a1` — was tried for cross-anchor score comparability and reverted; it broke the term's directionality.) The on-chart `indicator()` title is `SLT V1` (from `VERSION = "V1"`).

**Trading Hours — now a user input applied to every asset type (current):** previously the RTH session filter was profile-derived (`rth_auto`: `true` for MARKET/FUND/STOCK, `false` for FUTURES/CRYPTO so a 24h contract wouldn't get RTH-blacked-out). This caused the reported bug: futures signals fired with no hours restriction at all, any time of day. Replaced `rth_auto`/`useSessionFilter` entirely with a single always-enforced user input, `tradingHours` (`grp_hours`, `input.session`, default `"0930-1600:23456"`), read by `in_session`/`session_just_opened` via `time(timeframe.period, tradingHours)` for every profile uniformly — no per-profile exception and no on/off toggle. To run a symbol with no hours restriction, widen the session (e.g. `"0000-2359:1234567"`) in the input itself. See "Trading Hours" in [Key Design Decisions](CLAUDE.md#key-design-decisions).

**Gate change — velocity override extended to Gate 2:** `is_high_velocity` (`ADX > 35` rising 3 bars) now waives the Stretch penalty as well as the ATR-Fuel penalty. A strong, still-accelerating trend that has run past `ext_scale` ATR keeps its `final_score` intact instead of being docked −10/−20 and pushed below the star filter. The raw stretch tier is still shown as advisory (`gate2_raw`, gate message reads `🔥 VELOCITY` in place of `-N`); only the score subtraction and `active_gate_count` are suppressed. Implemented as `gate2_stretched = gate2_raw and not is_high_velocity`, mirroring the pre-existing `gate3_fuel` pattern.

**Dashboard — Strategy row removed:** the `active_strategy · <regime>` row is deleted. In the light build `active_strategy` is always `Trend Pullback`, and `regime` (the `SQUEEZE`/`BREAKOUT`/`EXHAUSTED`/`TREND`/`RANGE` string) had no consumer left after VWAP Fade — so `regime` and `sqz_bias` are deleted from `SLT.pine` too. Signal generation is now three sub-stages (strategy modules → router → dispatch).

**Win-rate change — unresolved reversals excluded (current):** previously a signal reversed before ever hitting PROFIT or LOSS was scored as a **loss** (the array push was `won ? true : false` with no third state). Now such an entry is excluded from `buy_results`/`sell_results`/`mod_results` entirely — win rate is a clean wins/(wins+losses) ratio over resolved entries only. Added `buy_lost_this_entry`/`sell_lost_this_entry` (parallel to the existing `*_won_this_entry` flags, set from `stop_reached`) and `buy_signals_fired`/`sell_signals_fired` (`var int`, uncapped, incremented at every signal open) to show total signals fired alongside the resolved win/loss count — dashboard format changed from `<rate>% [<w>/<t>]` to `<rate>% [<w>/<t>] (<N> fired)`. See [Win-rate tracking](CLAUDE.md#key-design-decisions).

**Day-end forced P&L resolution (current):** the Trading Hours input (previous entry) fixed signal *entries*, but PROFIT/LOSS *exits* still had no trading-hours gate at all — `current_pnl` kept evaluating on every confirmed bar regardless of session, so an off-hours price swing could still fire a PROFIT/LOSS label outside trading hours. Fixed by adding a day-end forced resolution, detected via `session_just_closed = not in_session and prev_in_session` (the first off-hours bar — same proven primitives as `session_just_opened`, just inverted). A first attempt tried detecting the last in-session bar directly via `time_close(timeframe.period, tradingHours)` returning `na`; that never actually fired in testing (`time_close` didn't behave like `time` does for this), so it was replaced with the one-bar-late `session_just_closed` approach, which reuses proven logic. `day_end_pnl` is computed from `close[1]` (the real last-in-session price) and, if the position is still open and unresolved, `day_end_win_hit`/`day_end_loss_hit` force it to resolve by that sign, OR'd directly into the existing `target_reached`/`stop_reached` booleans. Every downstream consumer — win-rate scoring, `pnl_exit_fired`, the PROFIT/LOSS alertcondition — picks this up with no new plumbing. The exit label shows the real P&L instead of the fixed target text for this path (`PROFIT +0.7%` / `LOSS -0.3%` rather than `PROFIT +1.0%` / `LOSS -1.0%`) and is anchored back to `bar_index - 1`/`close[1]` so it appears at the correct bar even though the code runs one bar late. Net effect: every entry now resolves (won or lost) within the trading day it opened — it can only go "unresolved"/excluded (see the win-rate change above) via a same-day reversal before the session closes, never by drifting unresolved across sessions. See "Trading Hours forced day-end resolution" in [Key Design Decisions](CLAUDE.md#key-design-decisions).

**Timeframe length scaling (current):** every bar-count lookback (EMA9/20/50, the profile SMA length, ATR/RSI/ADX(14), the squeeze BB/KC(20) lengths, the 20-bar RVOL fallback, `maxBarsFromFlip`) is now rescaled by `tf_scale = 120 / timeframe.in_seconds()` so each keeps covering the same wall-clock window on any chart interval, not just the tested 2-min chart. At `tf_scale = 1.0` (i.e. on a 2-min chart) nothing changes — see [Timeframe Length Scaling](CLAUDE.md#timeframe-length-scaling). Profile threshold/percentage values (`vwap_h_limit`, `adx_min`, `ext_scale`, `fuel_max`, RVOL tiers) are explicitly *not* rescaled — they have no comparable scaling law and stay validated only at 2-min. Implemented as inline `simple int` expressions (`len9`/`len14`/`len20`/`len50`/`len10`), not a shared helper function — a `scale_len()` function was tried first and failed to compile (`CE10123`), since Pine widens any user-defined function's result to `series`, which `ta.ema`/`ta.sma`/`ta.atr`/`ta.rsi`/`ta.dmi`/`ta.bb` reject for their `length` argument.

**Scoring change — MACD dropped (commit `9284b73`):** Component 5's MACD sub-component (was 5A, 8 pts, "MACD line same sign as trend direction") is removed. MACD-line sign ≈ `EMA12 > EMA26`, a near-duplicate of Component 1's EMA cascade, so it carried almost no independent signal. Its 8 points were redistributed proportionally (old 7:5 split) across the two survivors — RSI 7→12 (now 5A), Squeeze Release 5→8 (now 5B). Component 5 max stays 20 and the 105 pre-cap total is unchanged. The `ta.macd(close,12,26,9)` call, `macd_aligned` / `macd_pts`, and the `MACD: ✓/✗` field in `r_status` are all deleted; `r_status` tier thresholds re-cut to `16 / 12 / 8 / 3` for the new point distribution (cosmetic — the label never feeds `raw_score`).

**Code-review fix pass on day-end forced resolution (commits `0ba81e7`, `56dee3f`, `0cce0e7`):** a `/code-review` of the previous session's day-end forced-resolution commit (`343fd9a`) surfaced 8 findings; all were fixed. **Two high-severity correctness bugs:** (1) an entry opened on the exact bar a day-end trigger resolves has `close[1]` trivially equal to its own entry price, so it was being force-scored a meaningless exact-0% "win" with zero market exposure — fixed by adding `pnl_entry_bar` (the entry's opening `bar_index`) and requiring `pnl_entry_bar < bar_index - 1` in `day_end_close`, leaving such an entry unresolved instead. (2) day-end forced resolution silently never engaged for a `tradingHours` window widened to cover every bar (e.g. `"0000-2359:1234567"`, the very workaround this file itself suggests for 24h symbols) — `session_just_closed` can never fire there since `in_session` never goes false. Fixed by adding `day_end_fallback` (keyed off the pre-existing calendar-day-rollover detector `is_new_session`) OR'd into a new `day_end_trigger`; this also required re-keying the exit-label `is_day_end` flag off `day_end_win_hit`/`day_end_loss_hit` directly rather than `not in_session`, since the fallback can fire while `in_session` stays true throughout. **Three more fixes:** a pre-existing win-rate double-count (`buy_entry_open`/`buy_won_this_entry` weren't cleared at the opposite-signal close that consumed them, only at the next `session_just_opened`, so a stale resolved entry could get pushed into `buy_results` twice); a dashboard live-P&L staleness issue (`day_end_frozen_pnl` now freezes the displayed P&L at the day-end value instead of letting it keep drifting with the post-resolution close — normal in-session resolutions are unaffected); and two cleanup items (`day_end_pnl`'s division gated behind `day_end_trigger` instead of running every confirmed bar; the duplicated day-end label anchor/style logic between the PROFIT and LOSS blocks factored into `day_end_label_anchor()`/`exit_label_style()`). **One limitation documented rather than fixed:** `tradingHours` is designed for a single continuous range — a comma-separated multi-range session string (e.g. a lunch-break split) makes `session_just_opened`/`session_just_closed` fire at every internal gap, which would force day-end resolution mid-day; not safely fixable without parsing `tradingHours` itself, so it's called out in the input's tooltip and a code comment instead. See "P&L exits fire on target/stop hit", "Trading Hours forced day-end resolution", and "Win-rate tracking" in [Key Design Decisions](CLAUDE.md#key-design-decisions), and checklist items 18–19. User confirmed all three commits compile clean.

**Fired-count windowed to trailing 20 trading sessions (current):** `buy_signals_fired`/`sell_signals_fired` were plain `var int` lifetime counters — an all-time, never-reset, uncapped total for the whole loaded chart. On any chart with more history than `maxSignalsToTrack` (50) signals, the growing gap between `(N fired)` and the capped `[w/t]` resolved total became mostly an artifact of chart length rather than a meaningful "how many entries never resolved" signal. Replaced with a rolling window over the trailing `FIRED_LOOKBACK_SESSIONS` (20, new `const int`) **trading sessions** — chosen over raw calendar-day subtraction so weekend/holiday gaps don't shrink the window below ~20 real trading days, matching the existing `rvolLookbackDays` convention. Implementation: a new `session_seq` (`var int`) increments once per `is_new_session` (the same calendar-day-rollover detector the ATR Fuel gauge and Time-of-Day RVOL already rely on); `buy_fire_sessions`/`sell_fire_sessions` (`var array<int>`) push `session_seq` at every signal open (same hook the old counters used); every bar, a `while` loop trims entries off the front of each array once they fall `FIRED_LOOKBACK_SESSIONS` sessions behind — a plain front-trim is correct (and cheap — no full-array scan) because pushes always arrive in non-decreasing `session_seq` order. `buy_signals_fired`/`sell_signals_fired` are now `array.size(...)` of the trimmed arrays, recomputed every bar. **Known consequence, documented rather than "fixed":** because `[w/t]` is windowed by signal *count* (`maxSignalsToTrack`) and `fired` is now windowed by *session*, the two windows are independent — a low-frequency symbol could hold 50 resolved entries spanning more than 20 sessions, at which point `fired` can under-count relative to `[w/t]`. The previous lifetime-counter design trivially guaranteed `fired ≥ [w/t]`'s resolved total; that invariant no longer holds. See "Fired-count rolling window" in [Key Design Decisions](CLAUDE.md#key-design-decisions) and checklist item 24.

**Trade Signal verdict row added (current):** a new `Trade Signal` dashboard row, placed right after Setup Score (before Gates) — deliberately not grouped with the BUY/SELL Win Rate rows it reads from — turns the resolved win-rate stats for whichever direction fired *last* into a plain TRADE/CAUTION/SKIP/WAIT call, so the user doesn't have to mentally adjust the raw win rate for sample size and signal-reliability. Built from three new pinned constants (`VERDICT_MIN_SAMPLE=10`, `VERDICT_MIN_RESOLUTION=0.40`, `WILSON_Z=1.96`) and two new read-only helpers, `wilson_lower_bound()` (Wilson score lower-bound win rate, shrinks toward 50% for small samples) and `trade_verdict()` (applies the minimum-sample gate, then the resolution-rate floor, then the Wilson-adjusted 60/50 tier cutoffs already used for the win-rate row colors, and appends the implied expectancy `pnlTarget × (2×wlb−1)`). Tracks one direction at a time (`last_signal_type`) rather than a combined or dual-row design — a deliberate choice, since a live BUY and a live SELL are never both "the current call" simultaneously under strict alternation. Originally placed next to the win-rate rows and considered for the dashboard title row; both were rejected — the title row can't color-code the verdict (one Pine table cell = one text color for the whole string) and the user preferred it stand apart from Success Rate Tracking as its own line. `DASHBOARD_MAX_ROWS` stays at 36 (one more fixed row, still well under budget; Success Rate Tracking itself is unchanged at 3 rows). See "Trade Signal verdict" in [Key Design Decisions](CLAUDE.md#key-design-decisions) and checklist item 25.

**ATR Fuel row moved to Extended Metrics (current):** the `ATR Fuel` row used to be an always-on core dashboard row (white label, no color-coding), right before Setup Score. User feedback: it wasn't actionable for a trading decision, since its effect (Gate 3's −25 penalty) is already reflected in the Gates row, Setup Score, and now the Trade Signal verdict. Rather than delete it outright, it was moved into Extended Metrics (`extDash`), right after the existing `Volatility` row (ATR(14)/session range) it's thematically closest to — kept rather than removed because it's still the only place showing `🔥 VELOCITY OVERRIDE` (Gate 3's penalty waived by `is_high_velocity`), a state the Gates row's plain "NO ACTIVE" can't distinguish from a day with no fuel exhaustion at all. No logic changed — `fuel_used`/`fuel_max`/`is_high_velocity`/Gate 3 itself are untouched; only the dashboard row moved and is now gated on `extDash`.

**Dashboard title truly centered (current):** the header row (`SLT V1 | PnL ...`) was padded with trailing spaces out to 40 characters (`DASH_WIDTH`) before `text_halign = text.align_center` was applied — centering text-plus-padding visually left-justifies the real text instead. Removed the padding (and the now-unused `DASH_WIDTH` constant); the header cell's width already comes from the widest row below it via `table.merge_cells`, so no padding was needed for sizing. Purely cosmetic — no data/logic change.

**Doc/code-consistency fix pass (commit `d3e4a3f`):** a CLAUDE.md-vs-code diff found four items, all resolved. (1) Component 5A RSI: the bear 12-pt "high-conviction" tier fired at RSI < 45 (near-neutral) while the bull side needs RSI > 60 — tightened to RSI < 40 so the two sides mirror symmetrically about 50, matching the doc's "bear is the mirror". (2) CLAUDE.md's Setup-quality section still called `minScoreBuy`/`minScoreSell` "user inputs, range 0–70" — corrected to auto-resolved `min_score_auto` (45–55). (3) `extDash` tooltip claimed it adds the win-rate rows (they render under `enableSuccessRate` regardless) — reworded. (4) `showBg` removed outright: the parent's trend-background `bgcolor()` was dropped in the light build, so the pinned constant gated nothing; the SMA buffer band's `fill()` now shades unconditionally in SMA mode.

**Index Data Proxy for volume-less cash indices (current):** cash indices (SPX, IXIC/NDX, S&P/TSX, ...) report no volume, which silently disabled Component 2 (VWAP → `na` → 0 pts), pinned Component 3 to its 5-pt floor, capped Component 5B at 3, and made Gate 4 inert — dropping the realistic MARKET-profile score ceiling below `min_score_auto` (55) so the indicator barely fired on an index. Added an `INDEX DATA PROXY` block (after auto-tune resolution): when `syminfo.type == "index"`, the chart has never shown positive volume (`chart_has_volume`, a `var` latch), and the symbol family is recognized (`proxy_family_known` — S&P 500 / Nasdaq / S&P-TSX ticker patterns), one `request.security(proxy, timeframe.period, [ta.vwap(close), close, volume, ta.sma(volume, len20)], lookahead_off)` pulls VWAP + relative-volume from a liquid tracking ETF. `proxy_ticker` is a hardcoded symbol-family map (QQQ / XIC / SPY) — **no user input** (removed after first cut for the light-build minimal-panel principle; to hand-pick a proxy, edit the chain). `vwap` is reassigned once before Component 2 — real `ta.vwap` for volume-bearing symbols, `proxy_vwap_scaled` (proxy VWAP rescaled to index price units) when `use_proxy`, a session TWAP (`hlc3` running mean) for an index with no working/recognized proxy, and unchanged (`na`) for volume-less non-index symbols like forex. `vol_eff`/`vol_ma_eff` route proxy volume into `rel_vol_rolling`/`rel_vol_tod` and the Time-of-Day history. Dashboard: the Profile row shows `· <proxy> data` (or an orange `· ⚠️ volume-less → TWAP`), the Volume row's mode label gains a `proxy·` prefix. Non-repainting (`lookahead_off`, same contract as the daily ATR request); no signal-timing guarantee changes. See "Index Data Proxy" in [Key Design Decisions](CLAUDE.md#key-design-decisions) and checklist item 26.

**Index-proxy code-review fix pass:** a `/code-review` of the proxy feature raised four issues, all fixed. (1) `chart_has_volume` was a first-nonzero-bar `var` latch — a single sporadic tick-count bar on an aggregated index feed would permanently disable the proxy for the whole chart; changed to a monotonic latch that trips only on a **sustained ≥50% of the last 100 bars** carrying volume. (2) `session_twap` reset on `is_new_session` (calendar day), so a 24h index CFD averaged ~9h of overnight bars into the fair-value line before the cash open; re-anchored to the `tradingHours` RTH window via a local `time()` call. (3) the Time-of-Day slot average summed history entries with no `na` guard — a `na` `vol_eff` (proxy-less-index bar, or rare proxy gap) pushed into `tod_vol_history` poisoned that bar-slot's average for ~10 sessions; now `na` slots are skipped. (4) `rel_vol_rolling`/`rel_vol_tod` gained a `not na(vol_eff)` guard so a proxy per-bar volume gap (while the proxy vol-SMA is still valid) yields the neutral `1.0` rather than `na` → Component 3 = 0.

**Win-rate: one session window, W/L/U buckets (current — supersedes the two entries above about unresolved-exclusion and the fired-count window):** the win rate `[w/t]` was capped at `maxSignalsToTrack` (50) resolved trades by *signal count*, while `(N fired)` was windowed to `FIRED_LOOKBACK_SESSIONS` (20) *trading sessions* — two independent windows, so on a long/uneven history `fired` and the resolved total didn't compose (`fired − resolved` could go negative; the verdict's `resolution_rate = total/fired` could exceed 1.0). Replaced the `buy_results`/`sell_results` bool arrays **and** the `buy_fire_sessions` counters with **six `var array<int>`** — `buy_win_s`/`buy_loss_s`/`buy_unr_s` and the SELL trio — each holding the `session_seq` at **trade close**, front-trimmed every bar to the trailing `WINRATE_LOOKBACK_SESSIONS` (**15**, replacing `FIRED_LOOKBACK_SESSIONS`). Every closed trade lands in exactly one bucket: **W** (PROFIT fired), **L** (LOSS fired), or **U** (reversed same-day, never hit either — now its own bucket, no longer silently excluded). `buy_w`/`buy_l`/`buy_u` = the trimmed sizes; `success_rate_buy` = `W/(W+L)`, `totalTrades_buy` = `W+L`, verdict `fired` = `W+L+U` — one window, everything composes. Dashboard row changed from `<rate>% [<w>/<t>] (<N> fired)` to `<rate>%  ·  <W>W <L>L <U>↺` (rate grey when `W+L=0`). `add_result` deleted, `winrate_stats()` added; `calc_win_rate` kept for the (still bool-array, still signal-count-capped, still suppressed) per-module rows. `maxSignalsToTrack` now caps only `mod_results`. Inlined `while` trims (no UDF) to stay on the proven pattern. See "Win-rate tracking" in [Key Design Decisions](CLAUDE.md#key-design-decisions) and checklist items 13 / 24.

**Win-rate trim cleanup:** a `/code-review` of the W/L/U rework noted the six copy-pasted `while` front-trim blocks and that their inline comment's rationale ("a `while` inside a UDF is avoided") was unfounded — a UDF mutating an array passed by reference is legal and `record_module()` already does it. Folded the six blocks into one `trim_window(array<int> a, int cutoff)` helper (a `while` + `array.shift`), called once per bucket. No behaviour change; a future edit to the trim rule is now one place instead of six.

**Doc reorganization — CLAUDE.md trimmed to current-state reference (commits `9f4dfde`, `72a6e3b`, `7aedd5b`):** `CLAUDE.md` had grown to ~90 KB / 13.3k words, most of it history and reverted-experiment narrative already covered by this file. Trimmed to ~31 KB by cutting that narrative (reverted commit hashes, the `28→30→32→36` row-budget evolution, the full "if re-enabled" ORB spec for an inert module, repeated cross-section restatements) while keeping the entire current scope — every architecture section, all four reference tables (Profile Parameters, Auto-Tune, Scoring Components, Risk Gates), the key design decisions, Pine v6 gotchas, and the 25-item compile checklist. A review-diff against the pre-trim version then restored four pieces of over-trimmed *current* spec: the exact tier bounds for scoring Components 2 / 4 / 5A, and the Time-of-Day RVOL mechanics (same-slot comparison across N prior sessions, U-shape rationale, `<3`-session fallback, dashboard `TOD` / `20-bar` / `20-bar*` labels). `README.md` reworked from prose into emoji section headers + a feature list + a settings table — content unchanged. No `SLT.pine` change.

**Day-end P&L resolves on the final in-session bar at the live edge (commit `48d3a6e`):** `day_end_trigger` gained a third path, `day_end_onbar`. The two existing paths — `session_just_closed` (first off-hours bar) and `day_end_fallback` (calendar-day rollover) — both need a bar *after* the `tradingHours` close to fire on. On an RTH-only feed (a TSX/US equity whose data has no pre/post-market bars) watched live right at the close, consecutive bars straddle the overnight gap with `prev_in_session` still true, so `session_just_closed` never fires and there is no bar to carry the resolution until the next session's open — the PROFIT/LOSS bubble was delayed for hours (originally reported on a live SHOP/TSX chart where it simply never appeared). `day_end_onbar` fires *on* the last in-session bar itself: `barstate.islast and barstate.isconfirmed and in_session and bar_close_mins >= session_end_mins`, where `session_end_mins` is parsed once from `tradingHours` (`str.split` on `-` then `:`, taking the last range if comma-separated) and `bar_close_mins` is the bar's `time_close` in `syminfo.timezone`. On this path `day_end_pnl` reads `close` (not `close[1]`), the entry-age guard tightens to `pnl_entry_bar < bar_index` (vs `< bar_index - 1`), and `day_end_label_anchor()` places the label at `bar_index`/`close`. Gated on `barstate.islast` so it only ever touches the single live-edge bar — every historical day still resolves via `session_just_closed`, and on reload the same bar re-resolves through that path at an identical bar/price/%, so nothing moves. This is **not** the `time_close(timeframe.period, tradingHours)` approach that failed earlier (see the day-end forced-resolution entries above) — it reads the plain `time_close` bar-close timestamp and compares it to a parsed minute. A separate report that "futures fire before the last bar" on a 24h Gold contract was confirmed to be *correct* behaviour, not a bug — with `tradingHours = 0930-1600` the position resolves at 16:00 and the later 24h candles are outside the configured window, exactly as for a stock. User confirmed compiles clean; verified live on SHOP/TSX (bubble now sits on the 16:00 bar). See "P&L exits fire on target/stop" in [Key Design Decisions](CLAUDE.md#key-design-decisions) and checklist item 18.

**Win-rate window widened to 20 trading sessions (commit `9258d23`):** `WINRATE_LOOKBACK_SESSIONS` bumped `15 → 20`. This is the single `const int` that sets `winrate_cutoff = session_seq - WINRATE_LOOKBACK_SESSIONS`, feeding the six `trim_window()` calls that front-trim the W/L/U buckets, and transitively the `fired = W+L+U` total the Trade Signal verdict reads. Effect: the BUY/SELL Win Rate rows and the verdict now average over the trailing 20 trading sessions (~4 calendar weeks) instead of 15 — a larger resolved sample, so `VERDICT_MIN_SAMPLE` (10) is reached sooner and the Wilson lower bound is less aggressively shrunk. Still counted in *trading* sessions (`is_new_session`), so weekends/holidays don't shrink it. One-line code change plus the four `(15)` → `(20)` references in `CLAUDE.md` (§11, Win-rate tracking, checklist item 13). No logic restructure; the per-module `mod_results` store stays signal-count-capped at `maxSignalsToTrack` (50) and diverges slightly further from the session window (already documented). Not runtime-verified here — Pine compiles only in the TradingView editor; static-checked that nothing else assumes the old value.

**Oversized-bar entry filter (current):** on a volatile name at the RTH open, the 2-min SMA anchor flip-flops across its ATR band as expansion candles and their wicks straddle the line, and strict alternation (`b_fired`/`s_fired`) only blocks *same-direction* repeats — so BUY→SELL→BUY→SELL fired on consecutive bars (reported on a SpaceX/NASDAQ 2-min chart, ~6 signals in the first 14 minutes). The score filter can't help: an opening-thrust bar maxes Components 3/4/5 (volume, ADX, momentum) *while it prints*, so those signals genuinely cleared the star threshold at signal time. Gate 2 (Stretch) and Gate 3 (ATR Fuel) are the nearest existing dampers but Gate 2 keys off distance-from-`ema20` not bar range, Gate 3 is a cumulative session-vs-daily measure, and `is_high_velocity` (ADX>35 rising 3 bars) — which an opening spike trips — *waives both*. Added `oversizedBarATR` (pinned `2.2` in `FIXED BEHAVIOUR`, no panel input per the light-build principle) and `bar_not_oversized = na(atr) or atr <= 0 or (high - low) <= oversizedBarATR * atr`, where `atr` is the intraday timeframe-scaled `ta.atr(len14)` (**not** the daily `atr_14` used by the Fuel gauge). AND'd into both the Score and the (dead) Trend branch of `tp_long`/`tp_short` alongside `entry_is_fresh`. Chosen over a fixed N-bar signal cooldown because it's condition-based — it suppresses only while bars are genuinely wild and lets a clean signal fire the moment ranges normalize, with no dead time and no delay on a legitimate fast reversal. Non-repainting: `high`/`low` are final on the `barstate.isconfirmed` bar the Score branch already requires. A cooldown remains the fallback if clusters of *normal-range* bars are still seen whipsawing. See "Oversized-bar filter" in [Key Design Decisions](CLAUDE.md#key-design-decisions) and checklist item 12b.

**Oversized-bar filter scoped to the opening window (current — amends the entry above):** the all-day oversized-bar filter *degraded* observed win rate. Root cause: in strict alternation every signal is simultaneously the prior trade's exit, so blocking a signal on a big bar also blocks the reversal — a losing position rode through the expansion bar to a worse exit (or a deeper day-end forced resolution), and away from the open a big bar is often a legit momentum-continuation entry that got cut. Added `oversizedBarWindowBars` (pinned `15`, 2-min basis, tf-scaled at use as `oversized_window`) and a `var int bars_since_open` counter (`:= session_just_opened ? 0 : bars_since_open + 1`); `bar_not_oversized` gains a leading `not in_oversized_window` disjunct so the range test only bites for the first ~30 min of the RTH session, where the whipsaw cluster actually lives. Outside the window the filter is inert and the pre-existing gates (2/3) plus the star filter carry big-bar handling as before. No change to the threshold (`2.2`) or the two AND'd call sites. See "Oversized-bar filter" in [Key Design Decisions](CLAUDE.md#key-design-decisions) and checklist item 12b.

**Compile/execution optimization pass (current):** reported that both TradingView compile and per-bar execution had grown slow. Four changes, **all output-identical** — no signal, score, gate, win-rate, or dashboard-text change; verified by string-for-string equivalence of every affected dashboard cell. (1) **Proxy `request.security` symbol-gated:** the Index Data Proxy call used a hardcoded real ETF (`AMEX:SPY` default) as its symbol on *every* chart, so a stock/futures/ETF chart spun up a foreign SPY data feed each bar only to discard the result (`use_proxy` false for any non-index). Now `proxy_sec_sym = syminfo.type == "index" ? proxy_ticker : syminfo.tickerid` — non-index charts reference their own already-loaded series instead. `syminfo.type` is constant per chart so the requested symbol never changes across history; the rare volume-bearing index still fetches the proxy but `proxy_eligible` discards it exactly as before. Biggest execution win. (2) **`time(timeframe.period, tradingHours)` hoisted:** was evaluated three times per bar (`twap_in_sess`, `in_session`, `prev_in_session`), each re-parsing the session spec. Folded into one `int th_sess_time` computed in the "Value reference for Component 2" block (its first consumer, ahead of the session-window section) and reused below. (3) **Duplicate `ta.atr(len20)` collapsed** (`float atr20`, reused for both Keltner bands) — Pine does not common-subexpression-eliminate `ta.*` calls, so the textual duplicate was a second stateful ATR series. (4) **Display-only string building deferred to `barstate.islast`:** the per-component status strings and `gate_message` are shown only in the Extended Metrics dashboard, but their `str.tostring` interpolation ran on every historical bar. C2 alone did ~10 `str.tostring(vwap_dist_pct,"#.##")` per bar. Now `v_status`/`vol_status`/`a_status`/`r_status` hold just the bare tier label between the scoring engine and the dashboard, which re-attaches the numeric `(x.xx%)` / `(<rvol>x <mode>)` / `(ADX: …)` / `(RSI: … | SQZ: …)` detail; the entire `gate_message` builder (multiple `str.tostring`) moved from the RISK GATES section into the Gate Details block. Added `vwap_scoreable` (bool, hoisted from the old inline C2 guard) so the dashboard knows whether to append the distance. C1's `t_status` left as-is (interned-literal selection only, no per-bar `str.tostring`). User confirmed compiles clean; static-checked for v6 qualifier/scope correctness and output-string equivalence, not runtime-verified. Considered but deferred pending sign-off: deleting the inert ORB module and the dead per-module win-rate plumbing (`ModResults`/`mod_results`/`record_module`/`calc_win_rate`/`fmt_win_rate`) — the larger *compile*-time lever, but both are documented as intentional re-add scaffolding.

**Oversized-bar filter rolled back:** observed win rate degraded after the opening-window-scoped version above shipped. `oversizedBarATR`, `oversizedBarWindowBars`, `bar_not_oversized`, and `bars_since_open` removed from `FIXED BEHAVIOUR`/`SIGNAL GENERATION`; `tp_long`/`tp_short` no longer reference the filter in either branch. CLAUDE.md's "Oversized-bar filter" key-design-decision entry and checklist item 12b removed accordingly. No replacement mechanism at the time of rollback — see the Initial Balance rejection filter below for the entry that reclaims checklist slot 12b.

**Initial Balance (IB) rejection filter (current):** reviewed a Gemini deep-research summary of institutional intraday strategies for ideas to reduce false BUY/SELL signals that reverse into negative P&L; of the ~20 surveyed, most need order-book/tick/options-flow data this script has no access to (order-flow imbalance, footprint imbalance, iceberg absorption, Kyle's Lambda, 0DTE gamma/charm/vanna, VPIN, LULD halts) — Initial Balance Rejection was one of the few with a clean OHLCV-only implementation, and it directly extends the same "opening window is whipsaw-prone" insight the (now-rolled-back) oversized-bar filter was built on. Added `IB_MINUTES` (pinned `30` in `FIXED BEHAVIOUR`, no panel input) and a new tracking block in `SIGNAL GENERATION`, right where the oversized-bar filter used to sit: `ib_high`/`ib_low` extend through the first `IB_MINUTES` of each session (reset on `session_just_opened`) then `ib_locked` — the same extend-then-lock idiom the ORB module already uses, but keyed off wall-clock `time - ib_open_time` rather than a tf-scaled bar count, since ORB already proved that pattern out. `ib_up_broken`/`ib_down_broken` latch the first confirmed close beyond the range each session; that first break only fires with `rel_vol >= rvol_gate` (reusing Gate 4's liquidity bar rather than adding a second RVOL constant) via `ib_gate_long`/`ib_gate_short`, AND'd into both the Score and (dead) Trend branches of `tp_long`/`tp_short`. Once a side has broken with adequate volume, later same-side signals that session are untouched — this guards only the first, most whipsaw-prone poke past the range, not standing price action beyond it. Non-repainting: the range only extends on confirmed bars, and the break latches only under `barstate.isconfirmed`. See "Initial Balance (IB) rejection filter" in [Key Design Decisions](CLAUDE.md#key-design-decisions) and checklist item 12b.

**Initial Balance filter made observable (current — amends the entry above):** compiled clean, but with no dashboard footprint there was no way to tell whether the filter was ever actually engaging — a fair concern, since the gate only fires on the single first-break bar per session per direction and is easy to never observe by chance. Split `tp_long`/`tp_short` into a `tp_long_pre_ib`/`tp_short_pre_ib` (everything except the IB gate) and the final gated value, so a genuine block (`tp_*_pre_ib` true, `ib_gate_*` false) is distinguishable from "didn't qualify anyway." Added session-reset `ib_blocked_long_count`/`ib_blocked_short_count` (`var int`, incremented on a genuine block, zeroed on `session_just_opened`) and a new Extended Metrics "Initial Balance" row: `forming (<n>/30m)` while the range builds, then `<low>-<high>  ·  blocked <n>L <n>S` once locked, turning orange when either count is nonzero. Row-budget checked: fixed dashboard rows land around row 20 in this build's worst case (`orbEnabled`/`multi_module` both false), well inside `DASHBOARD_MAX_ROWS` (36) with the gate-details loop's own overflow guard untouched. No change to the gate's blocking logic itself. See "Initial Balance (IB) rejection filter" in [Key Design Decisions](CLAUDE.md#key-design-decisions) and checklist item 12b.

**CVD Divergence gate — Gate 5 (current):** second item from the Gemini deep-research review, after Initial Balance rejection. Of the 20 surveyed strategies, most order-flow techniques (OFI, footprint imbalance, iceberg absorption, Kyle's Lambda) need real bid/ask trade classification this script has no access to — Cumulative Volume Delta was the one with a clean OHLCV-only approximation: decompose each bar into 1-minute constituent bars via `request.security_lower_tf(syminfo.tickerid, "1", [close, open, volume])` and classify each sub-bar up/down volume by `close` vs `open` (the standard proxy public "volume delta" scripts use), sum into `cvd_bar_delta`, then run-sum into a session-cumulative `cvd`. Added as a genuinely new signal dimension — nothing in the existing 5-component score or 4 gates measures aggressive-flow direction independent of price — rather than folding it into an existing component, since Component 5's point budget was already carefully rebalanced once (the MACD → RSI/Squeeze reallocation) and a 5th gate keeps that budget untouched. `cvd_tf_ok = timeframe.isintraday and timeframe.multiplier > 1` gates the `request.security_lower_tf` call itself — `request.security_lower_tf` raises a **runtime error** (not just a wasted call) if asked for a `lower_tf` that isn't strictly below the chart's, so a 1-minute chart needed a hard guard, not just a "discard the result" pattern like the Index Data Proxy uses for non-index symbols. `cvd_tf_ok` being built from `simple` values (fixed per chart) makes gating the call itself safe, same class of condition an `input.bool` toggle would use. `cvd` gets its own self-contained session-open reset (`cvd_prev_in_sess` off the shared `th_sess_time`) because `session_just_opened` isn't declared until much later in the file — reusing the exact pattern `session_twap` already established for the same forward-reference problem. `gate5_divergence = (is_bull and cvd_slope < 0) or (is_bear and cvd_slope > 0)`, `cvd_slope = cvd - cvd[len14]` gated by `cvd_bar_count > len14` so the lookback can't reach across a session boundary — directional, unlike Gates 1-4, in the same sense Component 1's cascade check already is. `DIVERGENCE_PENALTY` (20) added alongside the other gate-penalty constants; `gate5_divergence` folded into `active_gate_count`, the gate-message builder, and the Gates-count row. Built observable from the start this time (the IB gate's first pass shipped silent and needed a follow-up commit for visibility): a new CVD Slope row in Extended Metrics distinguishes "n/a (chart ≤1m)" / "n/a (no volume)" / "warming up (n/`len14`)" from an actual `▲`/`▼` `format.volume` reading, turning orange the moment the gate fires. See "CVD Divergence gate" in [Key Design Decisions](CLAUDE.md#key-design-decisions), the Risk Gates table, and checklist item 7b.

**CVD loop fix — empty-array for-loop bug (current — amends the entry above):** hit `array.get() index 0 out of bounds, array size is 0` on bar 0. Root cause: `for i = 0 to array.size(cvd_ltf_c) - 1` assumed a C-style "skip if from > to," but Pine infers a for-loop's direction from its `from`/`to` values rather than skipping — on bar 0 (before `request.security_lower_tf` has returned any sub-bars) the array is empty, making the bound `-1`, and `for i = 0 to -1` still executes once, descending, at `i = 0`. `array.get(cvd_ltf_c, 0)` then throws on the genuinely-empty array. Fixed by wrapping the loop in `if array.size(cvd_ltf_c) > 0`. The two pre-existing `for i = 0 to array.size(...) - 1` loops (`words`, `gate_lines` in the Gate Details word-wrap) were checked and are safe without the same guard — both only ever run when their source string is non-empty, and `str.split` on a non-empty string always returns at least one token. Added as a new bullet in [Pine Script v6 Gotchas](CLAUDE.md#pine-script-v6-gotchas).

**Earnings-day IB widening (current):** third and final item from the Gemini deep-research review — Post-Earnings Announcement Drift (PEAD). A hard earnings-day block was rejected (see the original feasibility review) because it throws away legitimate post-earnings continuation trades; instead the failure mode targeted is the classic gap-and-reverse whipsaw, which is the exact same shape the Initial Balance filter already guards against, just running longer than a normal opening range on an earnings session. Added `IB_EARNINGS_MULT` (2, `FIXED BEHAVIOUR`) and `earn_actual = request.earnings(syminfo.tickerid, earnings.actual, barmerge.gaps_on, barmerge.lookahead_off)`. `gaps_on` was the key choice: the default `gaps_off` forward-fills a reported value across every subsequent bar, which would read non-`na` on literally every bar after the first-ever historical earnings report — useless for "is TODAY the report day." With `gaps_on` it's non-`na` only on the bar where a new datapoint actually posts. Queried against the chart's own symbol, so — unlike the Index Data Proxy's foreign-ticker call — there's no "wasted feed" cost from calling it unconditionally on every profile; the result is simply ignored except when `asset_category == "STOCK"`. `is_earnings_day` (`var bool`, reset on `session_just_opened`) naturally covers both earnings-release timings without special-casing them: a before-market report surfaces on the `session_just_opened` bar itself (the chart's first bar after publication, since `tradingHours` has no pre-market bars), and an after-the-close report surfaces on the *next* session's open — exactly the session where each report's reaction actually plays out. `ib_minutes_effective = is_earnings_day ? IB_MINUTES * IB_EARNINGS_MULT : IB_MINUTES` replaces the bare `IB_MINUTES` constant in the IB extend-while-building check and in the dashboard's "forming (n/Nm)" text. The Initial Balance row's label gains a trailing 📅 on a widened session so a longer-than-usual range reads as intentional, not a bug. See "Earnings-day IB widening" in [Key Design Decisions](CLAUDE.md#key-design-decisions) and checklist item 12c.

**P&L friction cost (current):** a second round of Gemini deep-research review of the script (after the Initial Balance / CVD Divergence / earnings-widening pass) flagged two remaining gaps: fixed-percentage VWAP bands instead of dynamic stdev bands, and zero-friction P&L tracking inflating win rates. The stdev-band rework was scoped as a separate, much larger initiative — it would replace Component 2's core logic and invalidate the calibrated `vwap_h_limit`/`vwap_e_limit` values across all 5 profiles, and has no clean answer for volume-less symbols (the proxy/TWAP path has no real per-trade volume distribution to compute a weighted stdev from) — deferred pending a dedicated design discussion. The friction-cost fix was small and mechanical by comparison: added `PNL_FRICTION_PCT` (0.05, `FIXED BEHAVIOUR`, a single conservative generic estimate rather than profile-scaled) and subtracted it from both raw P&L formulas — `current_pnl` and `day_end_pnl` — so `target_hit`/`stop_hit`, `day_end_win_hit`/`day_end_loss_hit`, and the win-rate W/L/U buckets they feed all reflect a realistic net outcome. Applied at the source (the two formulas) rather than at each consumer, so every downstream reader — including the dashboard header's `PnL <x>%` and the day-end PROFIT/LOSS label text — picks it up automatically. Net effect: a raw move now needs `pnlTarget + PNL_FRICTION_PCT` to trigger PROFIT (a LOSS triggers `PNL_FRICTION_PCT` sooner), and a position that closes exactly flat at day's end now correctly resolves as a small loss rather than a trivial win. Deliberately scoped to the live-tracking system only — the separate Signal P&L History (`signal_pnl`, shown on BUY/SELL labels) is purely informational, not used for target/stop or win-rate resolution, and was left as a raw price move. While implementing, found and fixed a pre-existing duplicate: the dashboard header's `live_pnl_pct` re-derived the same raw `(close - entry) / entry × 100` formula independently instead of reusing `current_pnl` — silently bypassing the new friction subtraction had it been left alone. Now reuses `current_pnl` directly (the day-end-frozen branch was already correct, since `day_end_frozen_pnl := day_end_pnl` picks up the already-adjusted value). See "P&L friction cost" in [Key Design Decisions](CLAUDE.md#key-design-decisions) and checklist items 11 / 11b.

**Code-review fix pass (current):** a `/code-review` of the accumulated session's diff (Initial Balance filter, CVD Divergence gate, earnings-day widening, P&L friction cost) surfaced 10 findings; 5 fixed, 5 deferred. Fixed: (1) `cvd_tf_ok = timeframe.isintraday and timeframe.multiplier > 1` didn't exclude seconds-based charts (e.g. `"30S"`) — `timeframe.isintraday` is true there too and `timeframe.multiplier` is the numeric multiplier (30), so the gate wrongly passed on a chart already finer than 1 minute, and `request.security_lower_tf(..., "1", ...)` would then request a *higher* timeframe than the chart's own — a runtime error per this file's own documented gotcha, not a silent no-op. Added `not timeframe.isseconds` to the gate. (2) `is_earnings_day` reset on `session_just_opened` (before checking `earn_actual`) silently dropped an after-the-close (AMC) earnings flag whenever the chart displays extended hours — the report's one non-`na` bar lands on the off-session post-market bar, and the flag would be cleared on the next *real* open bar before the check could re-arm it (the non-`na` bar had already passed). Reset moved to `session_just_closed` (the end of the *prior* session) instead, so the flag persists correctly across the gap. (3) `request.earnings` was called unconditionally on every bar for every profile even though its result is only used for STOCK — gated behind `if asset_category == "STOCK"` (safe: `asset_category` is `simple`, same class of condition as `cvd_tf_ok`), matching the "no wasted request call" convention already established for the Index Data Proxy. (4) `PNL_FRICTION_PCT` was subtracted independently in `current_pnl` and `day_end_pnl` (4 near-identical inline copies) — the review pointed out this is the exact duplication shape the session's own `live_pnl_pct` bug had just been found and fixed for; extracted a shared `net_pnl_pct(is_buy, entry, px)` function, following the same helper-extraction precedent as `trim_window()`. (5) CLAUDE.md's Compile-Check Checklist item 9 still said "all 4 gates active" after Gate 5 (CVD Divergence) had already been added elsewhere in this session's changes — updated to 5. Deferred (flagged, not acted on): the Initial Balance gate carries the same strict-alternation entry/exit interaction risk that got the earlier oversized-bar filter rolled back (narrower in scope here, but the same class of risk — worth watching for a win-rate regression the same way); the CVD `request.security_lower_tf` call runs even on volume-less symbols where its result can never be used (deferred because gating it on `chart_has_volume`, a `series` runtime latch, has not been proven safe for `request.*` call-gating in this codebase, unlike the `simple`-qualified `cvd_tf_ok`/`asset_category` conditions used elsewhere); `cvd_in_sess`/`cvd_prev_in_sess` exactly duplicate `twap_in_sess`/`twap_prev_in_sess`'s session-open latch pattern; the new Initial Balance range-latch block near-duplicates the pre-existing ORB module's extend-then-lock state machine (a shared helper is nontrivial in Pine v6 since a UDF can't reassign value-type globals); and the VWAP dashboard row's status-string formatting duplicates the same hand-rolled pattern already used by 4 other rows (pre-existing code, not introduced this session). Full findings reported via `ReportFindings`.

**VWAP Standard Deviation Bands — Component 2 (current):** the last unresolved item from the two rounds of Gemini deep-research review — fixed-percentage VWAP extension thresholds don't adapt to the day's actual volatility, so the same 1.5% move reads as routine on a violent day and extreme on a quiet one. Previously scoped as a bigger, riskier rework than the four additive gates (it replaces live scoring logic, not an independent opt-in filter) and deferred pending a concrete design for its blocking issue: volume-less symbols (the proxy/TWAP path) have no real per-trade volume distribution to derive a stdev from. Resolved that blocker by scoping this pass to **real-volume symbols only** (`chart_has_volume`) rather than trying to extend stdev-borrowing all the way into the proxy path in the same change — a materially bigger, riskier addition (would need a `request.security`-evaluated helper function carrying its own `var`-state accumulator inside the proxy's own context) left for a possible future pass. Added `vwap_sum_pv2`/`vwap_sum_v` — session-cumulative `Σ(volume × close²)` / `Σvolume`, the same manual-accumulator idiom TWAP/CVD already use — computing `vwap_variance = E[X²] - E[X]²` around the **existing** `vwap` value (not a second, re-anchored one from `ta.vwap`'s own banded overload, which would have required an explicit anchor argument and risked a subtle mismatch against the `vwap = ta.vwap(close)` value every profile's scoring has always been calibrated against). `vwap_stdev = sqrt(variance)` when positive, else `na`. `vwap_use_stdev = chart_has_volume and not na(vwap_stdev) and vwap_stdev > 0` picks the active unit; `vwap_h`/`vwap_e` resolve to the new `VWAP_STDEV_HEALTHY`/`VWAP_STDEV_EXTENDED` (`const float`, 1.0/2.0) when true, or the existing `vwap_h_limit`/`vwap_e_limit` (%, still profile-specific) when false — the tier logic itself (BOUNCE/HEALTHY/EXTENDED/REVERSION RISK/GRACE ZONE, including the 0.33/0.2/0.1 fractional sub-tiers) is completely unchanged, only the unit and the specific numbers swap. Notably, `VWAP_STDEV_HEALTHY`/`EXTENDED` are deliberately **not** profile-adaptive, unlike every other threshold in the Profile Parameters Table: each profile's pre-existing `vwap_e_limit / vwap_h_limit` ratio was already ≈2.0 across all 5 (MARKET 0.7/1.5, FUND 1.0/2.0, STOCK 1.8/3.5, FUTURES 1.5/3.0, CRYPTO 3.0/6.0) — those percentages were themselves an empirical stand-in for "roughly 1σ / roughly 2σ" per asset class, so a real, per-symbol, per-day stdev makes the profile-specific tuning redundant once one is available; a single shared statistical convention applies once volatility is measured directly instead of approximated. As a side effect, consolidated a code-review-flagged duplication while adding the third consumer of the same pattern: TWAP and CVD each kept an independent in-session/prev-in-session var-bool pair computing the identical `not na(th_sess_time)` condition — folded into one shared `early_session_just_opened` latch (`early_sess_active`/`early_sess_prev_active`) that all three (TWAP, CVD, and the new VWAP stdev accumulator) now reset on. Dashboard: VWAP Value row's numeric suffix reads `(x.xxσ)` or `(x.xx%)` depending on which path is live, so the active mode is always visible (same "observable, not silent" principle as the IB and CVD gates) — `vwap_dist` replaces `vwap_dist_pct` as the renamed, unit-agnostic distance variable. See "VWAP Standard Deviation Bands" in [Key Design Decisions](CLAUDE.md#key-design-decisions), the Profile Parameters Table, and checklist item 23b.

**Second code-review fix pass — 3 real regressions in commit `c78cb35` (current):** a `/code-review` of the pushed commit (IB filter, CVD gate, earnings widening, P&L friction, VWAP stdev bands) surfaced 4 findings; 3 were genuine regressions and fixed, 1 was already known and re-deferred. (1) **IB gate latch bug:** `ib_up_broken`/`ib_down_broken` were set on ANY first close beyond the range, whether or not that break passed the RVOL check — a low-RVOL fakeout got correctly blocked *and* incremented the latch, so the very next bar `ib_fresh_up_break` was already false and `ib_gate_long` resolved unconditionally true, permanently disabling the gate for the rest of the session after a single blocked poke. Directly contradicted the documented intent ("once a side has broken *with adequate volume*..."). Fixed: the latch now only sets when `rel_vol >= rvol_gate` on the same bar as the break, so a low-RVOL poke stays "fresh" (still gated) until a bar that actually clears the volume bar shows up. (2) **VWAP stdev accumulator never resets under a 24h-widened `tradingHours`:** `early_session_just_opened` (feeding `vwap_sum_pv2`/`vwap_sum_v`'s reset) was purely `tradingHours`-derived; with `tradingHours` widened to `"0000-2359:1234567"` (this script's own documented technique for round-the-clock CRYPTO/FUTURES, checklist item 17), `in_session` never goes false, so the trigger fires once at bar 0 and never again — turning the accumulator into a running total over the ENTIRE chart history while `vwap` (`ta.vwap(close)`) kept resetting daily per the exchange session regardless. `vwap_variance = E[X²] - E[X]²` then compared a multi-year `E[X²]` against a single day's `E[X]`, producing wildly wrong or silently-`na`'d (negative) variance — precisely for CRYPTO/FUTURES, the two real-volume profiles this feature is meant to help. CVD's own `cvd_slope = cvd - cvd[len14]` turned out to be immune to the same stuck-reset problem (a *difference* of two nearby cumulative sums cancels out an arbitrarily large shared base — VWAP's variance formula has no such self-cancellation). Fixed by OR'ing in `is_new_session` (calendar-day rollover, already computed earlier in the file for the daily ATR fuel gauge/TOD RVOL) as a fallback trigger — harmless on a normal RTH chart (the two triggers coincide on the same bar there), but bounds the accumulation window to at most one calendar day under a widened session. (3) **No warm-up guard on `vwap_use_stdev`:** unlike `cvd_slope_ready` (`cvd_bar_count > len14`), the VWAP-stdev path had no minimum-sample-size check — the first bar or two of every session traded a near-zero-sample variance, so a tiny price move off VWAP divided down to a wildly inflated sigma reading (e.g. "12σ"), pinning Component 2 to REVERSION RISK/WRONG SIDE at the open on every real-volume symbol, every single session (the most frequent of the four findings, recurring daily rather than only under an edge-case config). Fixed by adding `vwap_sample_count` (reset alongside the sums) and `vwap_stdev_ready = chart_has_volume and vwap_sample_count > len14`, reusing `len14` for consistency with CVD's exact warm-up window rather than inventing a new threshold; `vwap_use_stdev` now reads `vwap_stdev_ready and not na(vwap_stdev)`. (4) **Re-deferred, not new:** the CVD `request.security_lower_tf` call still runs on volume-less symbols where `cvd_eligible` can never be true — already documented as a known, deliberately-deferred inefficiency in the prior code-review pass (gating it on `chart_has_volume`, a `series` runtime latch, hasn't been proven safe for `request.*` call-gating in this codebase); left as-is. See "Initial Balance (IB) rejection filter" and "VWAP Standard Deviation Bands" in [Key Design Decisions](CLAUDE.md#key-design-decisions), and checklist items 12b/23b.

**V2 — five false-signal changes (current, uncommitted):** `VERSION` → `"V2"`; the pre-change script is frozen byte-for-byte as `SLT-V1.pine` for side-by-side win-rate comparison. Motivation: BUY/SELL signals that never reach `pnlTarget` and resolve negative. Review found the "Trend Pullback" module had no pullback condition at all (in SMA mode any bar while `is_bull` with `final_score ≥ 50` fires), no anti-chase cap with teeth (STOCK `ext_scale` 3.0 ATR / −10 lets a fully extended bar still score ~80), no higher-timeframe context, an unguarded opening window (the IB gate only guards the first break *after* `ib_locked`), and only three exits — a symmetric 1% target/stop, the opposite signal, or day-end — so a BUY whose bear-side signal never qualified rode the whole down move. The oversized-bar rollback lesson (blocking a signal blocks the prior trade's exit) shaped the design: **(3) structural exits first**, so every entry filter is exit-safe. **(1) Entry window** — no new entries while the IB is still forming (`ib_locked` false; the position is flat at open so nothing is left un-exited) or inside the last `ENTRY_CUTOFF_MINS` (30) before the `tradingHours` end (`mins_to_close` from the already-parsed `session_end_mins`/`bar_close_mins`; a late entry rarely reaches target and friction turns a flat EOD close into an L). **(2) Pullback-and-resume + anti-chase** — `pullback_ok_long = ta.barssince(low <= ema9) <= len10 and close > ema9` (mirror short), plus a hard block when `(close − ema9)/atr > CHASE_MAX_ATR` (1.5). C5A RSI re-tiered for a pullback entry: full 12 now goes to RSI 45–60 *rising* (recovering), 8 to 45–60 flat, 3 to > 60 (extended/late), 0 below 45 — V1 paid the full 12 for RSI > 60 rising, i.e. the chase; bear mirrors about 50 with `ta.falling`. **(3) Structural exits** — `atr_stop_hit` at `ATR_STOP_MULT` (1.5) × ATR-at-entry (`pnl_entry_atr`) folds into `stop_hit`; `flip_exit` (`FLIP_EXIT = true`) closes the tracked position on `trendDown`-while-BUY / `trendUp`-while-SELL, resolved W/L by the sign of `current_pnl` on that bar (`flip_win_hit`/`flip_loss_hit` OR'd into `target_reached`/`stop_reached`; excluded from `day_end_fallback`/`day_end_close`). A flip exit clears the exited side's `b_fired`/`s_fired` so the next same-direction flip can re-enter; an ATR/pct stop deliberately does not (re-buying a failed move). **W/L now bucket at resolution time** inside UPDATE SUCCESS RATE TRACKING (`buy_entry_open` drops there); the opposite-signal and session-open carryover paths can only see an unresolved entry → U. The `*_won_this_entry`/`*_lost_this_entry` latches are gone — with the flip exit unblocking a same-direction re-entry before the opposite signal, the latched result could have been clobbered. `day_end_frozen_pnl` → `exit_frozen_pnl`, frozen on *every* exit path (V1 froze only on day-end and let a target-resolved position keep drifting in the header). Labels: a pure target/stop keeps the fixed `±pnlTarget%` text; flip/ATR/EOD exits read `PROFIT/LOSS <actual%> (<reason>)`; `last_exit_reason`/`last_exit_pnl` feed a Last Exit row. **(4) Gate 6 — HTF Bias** — `htf_tf` = `"15"` under 15 min, `"60"` under 1 h, else `"D"` (simple string); one `request.security` returns `[ta.ema(close, HTF_EMA_LEN)[1], …[2]]` so both values come from the last *completed* HTF bar (non-repainting); `htf_up = close > htf_ema and htf_ema >= htf_ema_prev`, `htf_down` mirror; `gate6_htf = (is_bull and htf_down) or (is_bear and htf_up)` → `HTF_PENALTY` (20). `HTF_HARD_BLOCK = false` promotes it into the hard-block set with one flag. **(5) Hard blocks** — with `HARD_BLOCK_GATES = true` and any star mode other than "All Signals", an active Gate 5, an EXTREME stretch tier not waived by velocity (`stretch_extreme = gate2_stretched and stretch_penalty == STRETCH_EXTREME_PENALTY`), or Gate 6 when promoted, blocks the entry bar outright (a 75 − 20 still cleared 50). Filters chain window → structure → hard → IB, each with per-session `blocked nL nS` counters attributing a suppressed signal to the first filter that caught it (`tp_long_base` is the pre-filter condition; `tp_long_pre_ib` now means "after V2 filters, before IB"). Dashboard: Gate 6 in the count and `🧭 HTF BIAS (-20)` in Gate Details; five new Extended Metrics rows — Entry Window, Pullback, HTF Bias, Hard Blocks, Last Exit; `DASHBOARD_MAX_ROWS` 36 → 40. All tunables live in `FIXED BEHAVIOUR`. Not yet compiled or measured — first live pass should confirm the block counters engage and compare the W/L/U rows against `SLT-V1.pine` on the same chart.

**V2 rebuild — single EMA anchor, aggressive cleanup (current, uncommitted, supersedes the entry above):** the five-change V2 shipped on the dual-anchor base and the user reported the win rate "significantly dropped". Diagnosis: (a) mechanical — V1 sent every trade reversed before ±1% to the unresolved U bucket, V2's flip exit resolves those by sign, mostly as small losses, so the *rate* falls even with identical entries; (b) the 1.5-ATR stop is 2–3× tighter than the 1% target, so a lower rate is expected and the `target × (2·rate − 1)` expectancy formula was now wrong; (c) after an ATR stop the side stayed blocked until the opposite SMA flip, hours later, excluding every later pullback in the trend. User then asked for a redundancy pass, to remove everything not needed by the new logic, and for an anchor assessment. Decisions (AskUserQuestion): **EMA 9/slow only**, **aggressive cleanup**, **drop the index proxy**. `SLT.pine` rewritten from scratch (2974 → 1047 lines; 5 → 3 `request.*` calls; the dual-anchor V2 kept only as a scratch backup). **Removed:** SMA anchor + `anchorMode` input + buffer band + `anchor_ready` warm-up guard; ORB module and its 8 constants/plots/router/dashboard row; per-module win-rate (`ModResults`, `record_module`, `calc_win_rate`, `fmt_win_rate`, `strat_id`, `maxSignalsToTrack`); the `sigTime == "Trend"` branch and every pinned flag (`gateOn`, `showLabels`, `showCircles`, `enableSuccessRate`, `rvolMode`, `showSignalPnL`); Signal P&L history on labels (misleading once positions close on stop/flip); `maxBarsFromFlip`/`entry_is_fresh` (a pullback happens after the flip by design); the EMA-distance half of Gate 2 (`CHASE_MAX_ATR` supersedes it — Gate 2 is now trend-stretch-since-flip only); the Initial Balance RVOL gate + earnings-day widening (entries are already blocked while the range forms; Gate 4/C3 price in low volume); the Index Data Proxy and session TWAP; `b_fired`/`s_fired` strict alternation; the U bucket, `wilson_lower_bound`, and the resolution-rate verdict gate. **Kept:** 5-component scoring (with the V2 RSI re-tier), Gates 1/3/4/5, Gate 6 HTF bias (now hard), Time-of-Day RVOL, VWAP stdev bands, CVD, velocity override, timeframe scaling, day-end resolution (all three paths), P&L friction. **New:** `OPEN_BLOCK_MINS` replaces the IB-lock window test; **structural stop** `pnl_stop_price` fixed at entry = pullback `swing_low/high` (`len10`) ± `STOP_BUFFER_ATR`, clamped to `[STOP_MIN_ATR 1.5, STOP_MAX_ATR 3.0] × ATR`, drawn as a red step line while open, with `-pnlTarget` as a backstop; **one position at a time** with re-entry after `REENTRY_COOLDOWN_BARS` (`len10/2`) and a fresh EMA9 touch after the exit bar — no alternation; **stats rebuilt on expectancy**: paired `*_res_sess`/`*_res_pnl` arrays (net % per resolved trade, 20-session window), rows read `<rate>% · nW nL · Σ ±x.xx%`, `trade_verdict(total, avg)` = WAIT / TRADE (avg ≥ 0.10%/trade) / CAUTION (≥ 0) / SKIP. Dashboard `cell()` helper + `var table d` moved to global scope (Pine can't declare a function inside an `if`). Trend row shows EMA9; Anchor row `EMA 9/20 · HTF 15m ▲ UP`. CLAUDE.md rewritten for the new design (checklist 1–20). **Not yet compiled** — first paste should check the global `cell()` helper, the tuple-returning `dir_stats`, and the `"+#.##;-#.##"` sign formats.

**V2 parked; V1.1 = V1 + Σ net P&L (current, uncommitted):** the single-anchor V2 rebuild above compiled but the user reported results "still bad" and, of everything in V2, wanted only the Σ idea. Working file reverted to the frozen `SLT-V1.pine` (V2 rebuild kept only as a session scratch backup, not in the repo; the HISTORY entries above stay as the record of what was tried). `VERSION` → `"V1.1"`. **Σ:** per direction a paired `buy_all_s`/`buy_all_p` (session stamp / net %) list, pushed at the same two close sites as the W/L/U buckets — resolved trades push `buy_entry_result` (captured the bar `target_reached`/`stop_reached` fires as `day_end_close ? day_end_pnl : current_pnl`, i.e. the PROFIT/LOSS label's own figure), a U trade pushes `current_pnl` on the reversal bar (the loss the win rate hides — U is INCLUDED by user decision), the session-open carryover pushes `nz(result, 0)`. `trim_pair()` trims the pair in step with `winrate_cutoff`; `sum_pnl()` sums on the last bar. Win Rate rows now read `<rate>% · nW nL n↺ · Σ ±x.xx%`, coloured by the sign of Σ (lime > 0, yellow = 0, red < 0) instead of the 60/50 rate tiers. Trade Signal verdict untouched. ~80 lines added, no change to signals, exits, or W/L/U counts. CLAUDE.md restored to the V1 version and amended (Σ KDD paragraph, checklist 13/13b, Version). Not yet compiled.

**V1/V2 redefined around the untracked `SLT-GOOD-FIX.pine` snapshot (current, uncommitted):** user asked to make that file "V1" and the current working script "V2". Inspection (it had never been read before) showed `SLT-GOOD-FIX.pine` is an older snapshot than the previously-frozen `SLT-V1.pine` (`a360d58`): it predates the Initial Balance rejection filter, CVD Divergence gate (Gate 5), earnings-day IB widening, P&L friction cost, and VWAP standard-deviation bands, and also predates a compile-performance pass that moved per-bar `str.tostring` calls (VWAP/volume/ADX/momentum status suffixes, the gate-details message) off the hot path to build lazily on `barstate.islast` only — it otherwise matches the script as of commit `40b6f13` (win-rate window 15→20 sessions), ~214 lines of diff, all of it the perf-pass string-deferral plus one dead-code caching tweak (`atr20`, `th_sess_time`, `proxy_sec_sym`). It already carried `VERSION = "V1"` internally. Action: `SLT-V1.pine` overwritten with `SLT-GOOD-FIX.pine`'s content (the previous `a360d58`-based V1 snapshot is superseded — recoverable from git history at `9e9a839`/`a360d58` if ever needed); `SLT.pine`'s `VERSION` → `"V2"` (previously `"V1.1"` — no other code change). Net effect: **V1** = 5-component scoring, Gates 1-4 only (no CVD/Gate 5), Trading Hours, day-end resolution, index proxy, W/L/U win-rate window, fixed-% VWAP distance only, no P&L friction subtraction, no Σ. **V2** = all of that plus the IB rejection filter, Gate 5 (CVD Divergence), earnings-day IB widening, `PNL_FRICTION_PCT`, VWAP stdev bands, and the Σ net-P&L column — i.e. this file's full feature set as CLAUDE.md already documents it. `SLT-GOOD-FIX.pine` itself is left in place, now redundant with `SLT-V1.pine`. **Naming note:** a separate single-EMA-anchor rebuild was called "V2" earlier the same session, then parked; it was never committed and has no file in the repo, so it does not collide with this V2, but don't confuse the two if reviewing chat history. CLAUDE.md's Version section and two in-body Σ callouts updated; ROADMAP.md's note corrected. Not yet re-verified in the Pine Editor (only the `VERSION` string changed in `SLT.pine`, so no new compile risk).

**Σ moved from Win Rate rows to Extended Metrics (current, uncommitted):** user asked to relocate the Σ net-P&L figure off the always-visible BUY/SELL Win Rate rows. `buy_rate_display`/`sell_rate_display` and their colors reverted to the pre-Σ, purely rate-based form (`≥60%` lime, `≥50%` yellow, else red, grey at zero trades) — `buy_sum`/`sell_sum`/`buy_n`/`sell_n` moved out of the always-computed Win Rate block into the `if extDash` block, where two new rows, **Net P&L (BUY)** and **Net P&L (SELL)**, display `±x.xx%` (or `—` with no closed trades) coloured by the sign of Σ (lime/yellow/red/grey) — same underlying `buy_all_p`/`sell_all_p` arrays and `sum_pnl()` helper, just read and displayed in a different place. No change to what's tracked or how W/L/U bucket — only where the Σ figure is shown and how the Win Rate rows are colored. `extDash` tooltip updated to list the Net P&L rows. CLAUDE.md: Architecture item 13, the Σ Key Design Decision paragraph, and checklist 13/13b updated to match.

**Compile/runtime performance pass (current, uncommitted):** user asked to review `SLT.pine` for compile/runtime speed. Three fixes, all display/derivation-only — no scoring, gating, or signal-generation change:

1. **Index Data Proxy `request.security` gated behind `syminfo.type == "index"`.** Previously called unconditionally on every bar for every symbol — for the 4 non-index profiles (stock/ETF/futures/crypto, the large majority of charts), it pointed at the chart's own symbol/timeframe "to avoid a foreign feed," but that still paid the full `request.security` context-switch cost for a result (`proxy_vwap_raw`/`proxy_close_raw`/`proxy_vol_raw`/`proxy_vol_ma_raw`) that `use_proxy` already guaranteed would never be read there (`proxy_eligible` requires `syminfo.type == "index"`). `syminfo.type` is `simple` — the same class of condition `cvd_tf_ok` already uses to gate `request.security_lower_tf`, and `asset_category` uses to gate `request.earnings` — so wrapping the call itself in `if syminfo.type == "index"` (declaring the four outputs as `na`-initialized floats outside, assigned via `:=` inside) skips the call entirely on non-index charts rather than just discarding its result. `proxy_sec_sym` (the old ternary that pointed the call at `syminfo.tickerid` for non-index charts) is gone — no longer needed.
2. **`trade_verdict()` moved off the per-bar path.** The Wilson-lower-bound calculation (`sqrt`, several multiplications) plus its `str.tostring`-built verdict string ran on every historical bar, but its only consumer is the dashboard's "Trade Signal" row (`barstate.islast`, `enableSuccessRate` only). `verdict_wins`/`verdict_total`/`verdict_fired` and the `trade_verdict()` call itself moved into that dashboard block — same "compute on the last bar only" idiom already applied to every component status-string detail (VWAP distance, RVOL, ADX, RSI suffixes) in an earlier compile-perf pass.
3. **`live_pnl_pct`/`pnl_text` moved off the per-bar path.** The `str.format` call building the dashboard header's `PnL <x>%` figure ran every bar; its only consumer is the header itself, so it moved into the `barstate.islast` dashboard block alongside the trade-verdict fix.

CLAUDE.md: Index Data Proxy Key Design Decision updated to describe the new gating.

**Trade Signal verdict: dropped the per-trade expectancy figure (current, uncommitted):** user found the `(±x.xx%)` appended to the TRADE/CAUTION/SKIP tiers ("this % doesn't make sense and won't help") confusing rather than useful — asked for the bare verdict word instead, then picked that over two alternatives (Wilson win rate %, raw W/L count) offered via `AskUserQuestion`. `trade_verdict()` no longer computes `ev = target * (2*wlb - 1)` or takes a `target` parameter at all; the TRADE/CAUTION/SKIP tiers now return the bare word with no appended number. `WAIT (n/10)` and `SKIP (CHOPPY)` are unchanged (their parenthetical is a count/reason, not the confusing expectancy %). Call site updated to `trade_verdict(verdict_wins, verdict_total, verdict_fired)` (dropped the `pnlTarget` argument). CLAUDE.md: "Trade Signal verdict" Key Design Decision and checklist item 24 updated to match, including correcting a stale claim that the function still ran per-bar right after `last_signal_type` updates — the prior compile-perf pass had already moved the call into the `barstate.islast` dashboard block, which this doc pass caught and fixed.

**Trade Signal verdict mirrored in the dashboard header (current, uncommitted):** user asked to make the verdict visible without scrolling to its own row — considered feeding it back into scoring/entry gating first, but that was assessed and rejected (a hard entry gate keyed off the verdict creates a bootstrap/self-blocking loop: `SKIP`/`WAIT` would suppress the very signals needed to ever generate the resolved trades that could move the verdict off `SKIP`/`WAIT`, a materially worse failure shape than the IB/oversized-bar gates already rolled back, since those never touched P&L exits but this could stick a whole direction indefinitely). Went with a display-only change instead, chosen via `AskUserQuestion` over two other header mockups (verdict-colored text, a separate colored banner row): the header text now reads `SLT <VERSION> · <BUY/SELL> <verdict> | PnL <x%>` (unchanged `SLT <VERSION> | PnL —` before any signal has fired), with the header cell's **background** tinted by the verdict's color (`color.new(verdict_color, 75)`, a translucent wash) rather than the text — keeps the title fully legible while still color-coding at a glance. Required hoisting the `trade_verdict()` call (previously computed right next to its own row) up to just after `active_gate_count`, before `table.new`, so the header can read `verdict_text`/`verdict_color` before building `header_with_pnl`; the dedicated Trade Signal row further down now reuses those same two variables instead of calling `trade_verdict()` a second time. CLAUDE.md: Architecture item 13 (Dashboard), the "Trade Signal verdict" Key Design Decision, and checklist item 24 updated to describe the header and note the single shared computation site.

**Header background tint reverted (current, uncommitted):** user asked to "bring back title raw background color" — the verdict-tinted header background from the change above was reverted; `header_bg` (the `has_previous_signal ? color.new(verdict_color, 75) : color.new(color.gray, 40)` ternary) removed and the cell's `bgcolor` argument reverted to the original plain `color.new(color.gray, 40)` unconditionally. The verdict word stays in the header title text (`SLT <VERSION> · <BUY/SELL> <verdict> | PnL <x%>`) — only the background coloring is gone. `verdict_text`/`verdict_color` are still computed once near `active_gate_count` and reused by both the header text and the Trade Signal row (that hoist wasn't reverted, since the header still needs the verdict word). CLAUDE.md: Architecture item 13 and checklist item 24 updated to drop the background-tint description.

**Code-review fix pass on the three-commit session block (`6fd370d`/`58d1291`/`5c99a00`):** a `/code-review` of the header-verdict commit (reading the two adjacent commits for context, since they touch the same code) surfaced 2 findings, both real, both fixed. (1) **`enableSuccessRate` gate disagreement:** `header_verdict` was gated only on `has_previous_signal`, but `verdict_text` is only ever assigned inside `if enableSuccessRate` and otherwise stays `""` — so with `enableSuccessRate` flipped to `false` (a plain `bool` in FIXED BEHAVIOUR that CLAUDE.md documents as retunable there) the dedicated Trade Signal row correctly disappears while the header still rendered a dangling direction with a doubled space: `SLT V2 · BUY  | PnL +0.31%`. Two consumers, same underlying gate, disagreeing. Fixed by ANDing `enableSuccessRate` into `header_verdict`. Does not manifest at the pinned default (`true`). (2) **Proxy `request.security` gate didn't cover unrecognized indices:** the compile-perf pass gated the call on `syminfo.type == "index"`, but `proxy_ticker` falls through to `"AMEX:SPY"` for *any* unmatched index while `use_proxy` additionally requires `proxy_family_known` — so a DAX/FTSE/Nikkei chart still spun up a foreign SPY feed every bar for a result that could never be read, the exact waste that pass set out to remove. `proxy_family_known` is built from `str.contains(str.upper(syminfo.ticker))`, i.e. `simple`, so it can safely join the gate: `if syminfo.type == "index" and proxy_family_known`. No behavior change either way — those symbols take the session-TWAP path, which is driven by `proxy_eligible`, not by the request. Also corrected two stale comments the review flagged: the header's "width comes from the widest content in the rows below it" (the header itself can now be widest, e.g. `· SELL SKIP (CHOPPY)`), and CLAUDE.md's Index Data Proxy bullet, which had long stated `use_proxy = proxy_eligible and proxy_ok`, omitting the `proxy_family_known` term that finding 2 turns on.

**Trade Signal row removed from the dashboard (current, uncommitted):** now that the verdict shows in the header title, user asked to drop the dedicated "Trade Signal" row underneath Setup Score — it was redundant with the header. Removed the `if enableSuccessRate` block that rendered the row (`trade_signal_display`/`trade_signal_color` + the two `table.cell` calls); `row += 1` for it is gone too, so every row below shifts up by one when Extended Metrics/Success Rate are off. `verdict_text`/`verdict_color` are untouched — still computed once near `active_gate_count`, before `table.new`, but now consumed only by the header (`header_verdict`). No change to `trade_verdict()` itself, the Wilson/sample-size/resolution logic, or the header text/format. CLAUDE.md: Architecture item 13, the "Trade Signal verdict" Key Design Decision, and checklist item 24 updated to describe the verdict as header-only with no dedicated row; the `extDash` tooltip's mention of the Trade Signal row also dropped.

**Day-end forced P&L resolution removed (current, uncommitted):** user asked to remove end-of-day P&L resolution entirely — a position still open when the trading-hours session ends is now left alone rather than force-closed by the sign of its P&L. Deleted the whole "DAY-END FORCED RESOLUTION" block: `day_end_fallback`, the `session_end_mins`/`bar_close_mins`/`reached_session_end` parsing, `day_end_onbar`, `day_end_trigger`, `day_end_pnl`, `day_end_entry_ok`, `day_end_close`, `day_end_win_hit`/`day_end_loss_hit`, and the `day_end_frozen_pnl` var (plus its three reset sites). `target_reached`/`stop_reached` collapse back to what were the `normal_target_reached`/`normal_stop_reached` conditions (in-session target/stop hit only) — the `normal_*` names are gone too, since there's no longer a second path for them to be "normal" relative to. `day_end_label_anchor()` (the helper that anchored a day-end label back to the correct prior bar) is deleted; the PROFIT/LOSS label blocks now always anchor at `bar_index`/`anchor_line_price`, same as any other in-session exit. The Σ net-P&L capture (`buy_entry_result`/`sell_entry_result`) simplifies from `day_end_close ? day_end_pnl : current_pnl` to just `current_pnl`. Net behavioral effect: a position that never hits `±pnlTarget` before the session closes no longer gets a synthetic PROFIT/LOSS label or a forced W/L in the win-rate buckets — it keeps drifting with the close (dashboard header included) until either the opposite signal closes it normally or the next `session_just_opened` carryover reset buckets it U, exactly like a same-day reversal. This also removes the 24h-`tradingHours` calendar-rollover fallback and the live-edge "resolve on the last confirmed bar" path, since there's no longer anything to resolve early. `ARCH/SLT-V1.pine` still has the old day-end-forced-resolution behavior (it predates this change) — flagged in CLAUDE.md's Version section as a new V1/V2 divergence for future side-by-side comparisons. CLAUDE.md: Architecture item 10, the "P&L friction cost" and "P&L exits fire on target/stop" (renamed, no longer "...or are forced at day's end") Key Design Decisions, the Win-rate tracking and Σ paragraphs, checklist items 11/11b (day-end wording dropped) and 18 (rewritten; old items 18-19 collapsed into one, renumbering every item after down by one, i.e. old 20-25 → 19-24), and the two P&L Exit / Trading Hours input tooltips, all updated to match. Not yet re-verified in the Pine Editor.
